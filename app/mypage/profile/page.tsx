"use client";
import{FormEvent,useEffect,useState}from"react";import{AccountGuard,AccountPage}from"../../../src/components/account";import{Shell}from"../../../src/components/layout";import{Profile,useStore}from"../../../src/context/StoreContext";
export default function ProfilePage(){const s=useStore();const[form,setForm]=useState<Profile>(s.profile);useEffect(()=>setForm(s.profile),[s.profile]);function submit(e:FormEvent){e.preventDefault();s.saveProfile(form);s.notify("会員情報を保存しました")}return <Shell><AccountGuard><AccountPage title="会員情報"><form className="settings-form" onSubmit={submit}><label>名前<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>メールアドレス<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>電話番号<input type="tel" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><button className="primary" type="submit">保存する</button></form></AccountPage></AccountGuard></Shell>}
