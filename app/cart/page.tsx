"use client";import Link from"next/link";import{useState}from"react";import{products}from"../../src/data/products";import{ProductCard,ProductImage,Quantity}from"../../src/components/product";import{Shell,yen}from"../../src/components/layout";import{useStore}from"../../src/context/StoreContext";
export default function Cart(){const s=useStore();const[code,setCode]=useState("");const[applied,setApplied]=useState(false);const discount=applied?Math.round(s.subtotal*.1):0;const shipping=s.subtotal>=2000||!s.count?0:220;const total=s.subtotal+shipping-discount;return <Shell><div className="container cart-page"><section><h1>カート <i>{s.count}</i></h1><p>商品の合計金額や配送方法をご確認ください</p>{s.count===0?<div className="empty"><h2>カートは空です</h2><p>気になる商品を見つけてお買い物を始めましょう。</p><Link className="primary" href="/products">商品を見る</Link></div>:<><div className="cart-table"><div className="cart-head"><span>商品</span><span>単価（税込）</span><span>数量</span><span>小計</span></div>{s.items.map(i=><div className="cart-row" key={i.product.id}><ProductImage product={i.product}/><div><b>{i.product.name}</b><small>{i.product.category}</small><button onClick={()=>s.remove(i.product.id)}>削除</button></div><span>{yen(i.product.price)}</span><Quantity value={i.quantity} onChange={n=>s.change(i.product.id,n)}/><b>{yen(i.product.price*i.quantity)}</b></div>)}</div><button className="clear" onClick={s.clear}>カートを空にする</button></>}<section className="section"><h2>ついで買いにおすすめ</h2><div className="product-grid home-grid">{products.slice(12,18).map(p=><ProductCard p={p} key={p.id}/>)}</div></section></section><aside className="summary"><h2>ご注文内容</h2><p><span>小計（{s.count}点）</span><b>{yen(s.subtotal)}</b></p><p><span>配送料</span><b>{shipping?yen(shipping):"無料"}</b></p><p><span>割引</span><b className="green">-{yen(discount)}</b></p><hr/><div className="total"><span>合計（税込）</span><b>{yen(total)}</b></div><p><span>獲得予定ポイント</span><b className="price">{Math.floor(total/100)}pt</b></p><Link className="primary wide" href="/checkout">ご購入手続きへ</Link><Link className="outline wide" href="/products">買い物を続ける</Link><div className="coupon"><h3>クーポンを利用する</h3><input value={code} onChange={e=>setCode(e.target.value)} placeholder="クーポンコードを入力"/><button onClick={()=>setApplied(code.toUpperCase()==="WELCOME10")}>適用</button>{applied&&<small>10%OFFを適用しました</small>}</div><div className="shipping-progress">🚚 <b>¥2,000以上のご注文で配送料無料になります！</b><p>あと {yen(Math.max(0,2000-s.subtotal))} で送料無料</p><progress value={Math.min(s.subtotal,2000)} max="2000"/></div></aside></div></Shell>}
